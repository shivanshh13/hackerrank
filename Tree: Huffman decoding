void decode_huff(node * root, string s) {
    node* cur = root;
    for(char c : s){
        if(c=='0') cur = cur->left;
        else cur = cur->right;
        
        if(cur->left == NULL && cur->right == NULL){
            cout << cur->data;
            cur = root;
        }
    }
}
