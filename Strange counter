#include <stdio.h>
#include <math.h>

long strangeCounter(long t) {
    long cycle = 3;
    while (t > cycle) {
        t -= cycle;
        cycle *= 2;
    }
    return cycle - t + 1;
}

int main() {
    long t;
    scanf("%ld", &t);
    printf("%ld\n", strangeCounter(t));
    return 0;
}
